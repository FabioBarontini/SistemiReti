import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
export default async function AdminPage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const {data:p}=await s.from('profiles').select('role,display_name').eq('id',user.id).maybeSingle();if(p?.role!=='teacher')redirect('/synora');const {data:rows}=await s.from('game_progress').select('user_id,current_room,completed_rooms,hints_used,errors,updated_at').order('updated_at',{ascending:false});return <main className="synora-shell"><div className="grid-bg"/><header className="topbar"><div className="brand">SYNORA // CONTROL ROOM</div><div className="status"><span className="dot"/> TEACHER NODE</div></header><section className="dashboard"><div className="eyebrow">LIVE MONITOR</div><div className="dash-title">Stato delle squadre</div><div style={{marginTop:25,overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse',background:'rgba(7,13,21,.8)'}}><thead><tr>{['Nodo','Stanza','Completate','Indizi','Errori','Ultima attività'].map(x=><th key={x} style={{textAlign:'left',padding:15,borderBottom:'1px solid rgba(126,188,255,.16)',color:'#79dfff',fontSize:10,letterSpacing:'.12em'}}>{x}</th>)}</tr></thead><tbody>{(rows??[]).map(r=><tr key={r.user_id}>{[r.user_id.slice(0,8),`0${r.current_room}`,`${r.completed_rooms}/8`,r.hints_used,r.errors,new Date(r.updated_at).toLocaleString('it-IT')].map((x,i)=><td key={i} style={{padding:15,borderBottom:'1px solid rgba(126,188,255,.08)',color:'#aac0cf',fontSize:12}}>{x}</td>)}</tr>)}</tbody></table></div></section></main>}
